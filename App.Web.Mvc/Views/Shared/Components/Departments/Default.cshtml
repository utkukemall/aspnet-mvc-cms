
@model IEnumerable<Department>

@{
    foreach (Department department in Model.OrderBy(d => d.Name))
    {
        <li><a class="dropdown-item" asp-controller="DepartmentPost" asp-action="Create" asp-route-id="@department.Id">@department.Name</a></li>
    }
}